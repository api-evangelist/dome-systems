generated: '2026-07-18'
method: searched
source: https://github.com/dome-systems/releases/releases + https://github.com/dome-systems/homebrew-tap
name: dome
description: Dome Platform CLI — AI agent governance (register agents, govern tool and model calls, author Cedar policy, query audit).
version: 0.1.3
license: Apache-2.0
homepage: https://domesystems.ai
docs: https://docs.domesystems.ai
install:
- method: homebrew
  command: brew install dome-systems/tap/dome
- method: github-releases
  command: 'download dome_<version>_<os>_<arch>.tar.gz'
  platforms: [darwin_amd64, darwin_arm64, linux_amd64, linux_arm64]
command_groups:
- group: Session
  description: Browser-based SSO login and multi-context switching across orgs and tenants.
  commands: [login]
- group: Connect
  description: Register and manage AI agents, issue/rotate agent API keys, configure gateway backends, browse tool catalogs.
  commands: [agents register, agents]
- group: Model (Broker)
  description: Manage LLM connections, routing pools, membership, and cost quotas.
  commands: [model add, model list, model get, model update, model rm, model providers, model oauth-connect, model oauth-disconnect, model pool create, model pool list, model pool get, model pool update, model pool rm, model pool set-default, model pool move, model pool member add, model pool member list, model pool member update, model pool member rm, model quota list, model quota set, model quota rm]
- group: Tool (Gateway)
  description: Attach and manage backends/tools on the Tool Gateway.
  commands: [tool]
- group: Secure (Authorization)
  description: Author Cedar authorization rules, validate and deploy rule bundles, simulate decisions, and rollback.
  commands: [rules validate, rules deploy, rules simulate, rules rollback]
- group: Operate (Audit)
  description: Query, stream, and export audit events across your workspace.
  commands: [audit]
- group: Usage & cost
  description: Query durable LLM token usage and cost attribution.
  commands: [usage]
- group: Settings
  description: Manage workspaces, membership/roles, act-as enforcement, verification providers, and platform API keys.
  commands: [workspaces]
- group: Developer
  description: Run the built-in MCP server for Claude and other MCP clients; provision sandbox environments.
  commands: [mcp, sandbox]
notes:
- The CLI auto-loads local .env files.
- 'Context-aware: commands resolve the active workspace from context (redundant workspace positional dropped in v0.1.3).'
- Command surface reconstructed from public GitHub release notes (v0.1.0-v0.1.3); the full reference lives behind the access-gated docs.
